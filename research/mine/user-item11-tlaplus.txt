You are executing spec /Users/Shared/lua-lunet/vrr-core/.tmp/specs/item11.md. Read that file first and follow it exactly. Repo root: /Users/Shared/lua-lunet/vrr-core. TIMEBOXED ~10 minutes: if any step is not easy (no Java and brew install is slow, release jar download fails), skip it and record why — do not fight the tooling. Add the tlaplus/tla2tools submodule at tools/tla2tools via `git submodule add` (this repo-level git work is explicitly approved; `git add` only, NEVER commit, and do not touch formal/ or any other tracked files). Download the released tla2tools.jar to .tmp/tla-hello/ and run TLC on a tiny Hello.tla/Hello.cfg hello-world spec there. Append results to .tmp/research/tool-kick-tires.md under a "## TLA+" heading (create the file; the item12 agent may also write to it — append only, do not truncate). Final report under 6 lines: submodule added y/n + path, TLC ran y/n + verdict, skip reasons. No content dumps.
