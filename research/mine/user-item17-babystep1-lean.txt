You are executing spec /Users/Shared/lua-lunet/vrr-core/.tmp/specs/item17.md. Read that file first and follow it exactly. Repo root: /Users/Shared/lua-lunet/vrr-core. Platform: darwin macOS, zsh. TIMEBOX ~40 min. Lean 4.33.1 via ~/.elan/toolchains/leanprover--lean4---v4.33.1/bin/lean (NOT on PATH). Draft with the Leanstral API (labs-leanstral-1-5-1) exactly per spec — key read SILENTLY via awk from .env, never print/copy/grep it; you may reuse/adapt .tmp/mistral-lean/ask_lean.py into .tmp/lean-upaxos/. If item16's blog sources already exist under .tmp/blogs/, use them to state definitions faithfully to the user's UPaxos material (check .tmp/blogs/ first); else work from the summary embedded in the spec. Scope is deliberately tiny: eras, ballots, Config ⟨Q_I,Q_II⟩, Overlap, the three overlap invariants, the 3-zone upgrade instance verified by decide/Finset, the FixableIn monotonicity micro-fact, one negative control. NO liveness, NO full Paxos agreement proof — mark the honest scope in a header comment. Compile-fix iteratively; count fix rounds (model vs you). No git; write only .tmp/lean-upaxos/ and .tmp/research/lean-upaxos-babystep1.md. Final report under 6 lines: compiled y/n, lemmas proven count, instance verified y/n, fix rounds model/agent, output paths. No content dumps, no key echoes.
