You are executing spec /Users/Shared/lua-lunet/vrr-core/.tmp/specs/item05.md. Read that file first and follow it exactly. Constraints: all work stays under .tmp/; run NO git commands; create/modify nothing outside .tmp/; never read or print .env. Verify the bundle has exactly 15 files (README.md, 7 PDFs in papers/, 7 markdowns in ocr-text/) before running gh. Then run the `gh gist create --private` command from inside the bundle directory as written in the spec. Report back in under 5 lines: the gist URL and the number of files uploaded. Nothing else.
