You are executing spec /Users/Shared/lua-lunet/vrr-core/.tmp/specs/item06.md. Read that file first and follow it exactly. Repo root: /Users/Shared/lua-lunet/vrr-core. Append the exact "## Subagent delegation" section from the spec to the end of AGENTS.md, altering nothing else in the file. Then `git add AGENTS.md` but NEVER commit (the user handles commits). Verify the section appears exactly once and `git diff --cached AGENTS.md` shows only the appended hunk. This edit outside .tmp is explicitly approved for this item ONLY. Final report under 4 lines: added y/n, diff stat. No content dumps.
