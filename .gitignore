/target/
Cargo.lock
# Maelstrom run artifacts and per-node durable state.
/maelstrom/store/
/.state/
# Scratch and item ledger. Never committed (decision P1).
.tmp/

.DS_Store
.claude
.tmp/
.env

# Lean build artifacts (regenerable via lake build)
.lake/
# Vendored dependencies (regenerable via lake build)
research/lean-solve-auto/duper/

# Zig build cache (regenerable via zig/build.sh)
.zigcache/

# Promptfoo config and cache (panel review)
.promptfoo/
