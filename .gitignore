/target/
Cargo.lock
# Maelstrom run artifacts and per-node durable state.
/maelstrom/store/
/.state/
# Scratch and item ledger. Never committed (decision P1).
.tmp/

.DS_Store
.claude
.tmp/
.env

# Lean build artifacts (regenerable via lake build)
.lake/
# Vendored dependencies (regenerable via lake build)
research/lean-solve-auto/duper/

# Zig build cache (regenerable via zig/build.sh); zig test's tmpDir root
.zig-cache/

# Promptfoo config and cache (panel review)
.promptfoo/

# Generated presentation media: the sources are scripts/make_slides.py,
# scripts/generate_podcast.py, scripts/export_video.py and
# docs/animation/podcast_script.md; every audio, video, frame, render and
# deck file under docs/animation/ and docs/slides/ is an output.
docs/animation/audio_chunks/
docs/animation/video_frames/
docs/animation/podcast.mp3
docs/animation/cues.json
docs/animation/cues.js
docs/animation/audit.log
docs/animation/uvrr_educational_explainer.mp4
docs/slides/renders/
docs/slides/*.pptx
docs/slides/*.pdf

.zigcache
